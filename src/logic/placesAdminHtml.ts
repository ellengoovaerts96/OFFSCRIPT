import type { PlaceAdminDetail, PlaceAdminFilterOptions, PlaceAdminFilters, PlaceAdminSummary } from "../data/placesAdminRepository.js";
import { escapeHtml } from "./sourcesAdminHtml.js";

function page(title: string, content: string): string {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#17201d"><meta name="apple-mobile-web-app-capable" content="yes"><meta name="apple-mobile-web-app-title" content="TUUTI"><link rel="manifest" href="/admin-assets/manifest.webmanifest?v=3"><link rel="apple-touch-icon" href="/admin-assets/tuuti-dashboard-gsm-v2-180.png"><link rel="icon" type="image/png" href="/admin-assets/tuuti_dashboard_flavicon.png"><title>${escapeHtml(title)} · TUUTI</title><style>
  :root{font-family:Inter,ui-sans-serif,system-ui,sans-serif;color:#102a24;background:#f2ebd9;--forest:#102a24;--rouge:#d95b32;--sand:#f2ebd9;--paper:#fffaf0;--muted:#68705a}*{box-sizing:border-box}body{margin:0;min-width:320px;background:radial-gradient(circle at 90% 0,rgba(217,91,50,.08),transparent 28rem),var(--sand)}a{color:inherit}header{padding:18px max(18px,calc((100% - 1180px)/2));border-bottom:3px solid var(--rouge);color:var(--sand);background:var(--forest)}header a{text-decoration:none}.brand{display:flex;align-items:baseline;justify-content:space-between;gap:20px}.wordmark{font-size:24px;font-weight:760;letter-spacing:.3em}.wordmark i{color:var(--rouge);font-style:normal}.brand-note{color:#d9d2bd;font-size:12px;letter-spacing:.06em}main{width:min(1180px,calc(100% - 32px));margin:38px auto 60px}h1,h2,h3{font-family:Canela,Georgia,serif;font-weight:500}h1{margin:0;font-size:clamp(34px,5vw,50px);line-height:1.05}h2{margin:0 0 14px;font-size:24px}h3{margin:0 0 7px;font-size:19px}.topbar,.actions{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap}.topbar{margin-bottom:24px}.meta{color:var(--muted);font-size:14px;line-height:1.5}.button,button{display:inline-flex;min-height:42px;align-items:center;justify-content:center;padding:9px 15px;border:1px solid var(--forest);border-radius:5px;color:var(--sand);background:var(--forest);font:inherit;font-weight:680;text-decoration:none;cursor:pointer}.button.secondary,button.secondary{color:var(--forest);background:transparent}.button.danger{border-color:#9b3d35;color:#9b3d35;background:transparent}.button:hover,button:hover{border-color:var(--rouge);background:var(--rouge);color:white}.panel,.place-card{border:1px solid rgba(16,42,36,.18);border-radius:8px;background:rgba(255,250,240,.94);box-shadow:0 8px 24px rgba(16,42,36,.05)}.panel{padding:24px;margin-bottom:18px}.filters{display:grid;grid-template-columns:2fr repeat(3,1fr);gap:14px;align-items:end}label{display:grid;gap:7px;color:var(--forest);font-size:12px;font-weight:720;letter-spacing:.025em;text-transform:uppercase}input,select{width:100%;min-height:42px;padding:9px 11px;border:1px solid rgba(16,42,36,.3);border-radius:4px;background:#fffdf7;font:inherit}.places{display:grid;gap:12px}.place-card{display:grid;grid-template-columns:minmax(220px,1.3fr) minmax(180px,1fr) minmax(170px,.8fr) auto;gap:20px;align-items:center;padding:19px;text-decoration:none}.place-card:hover{border-color:var(--rouge);transform:translateY(-1px)}.place-card h2{margin:0 0 5px;font-size:21px}.tags{display:flex;gap:6px;flex-wrap:wrap}.tag,.badge{display:inline-flex;padding:4px 8px;border-radius:999px;font-size:12px}.tag{background:#e9e2cf}.badge{font-weight:750;text-transform:capitalize;background:#dce1c8}.badge.draft{background:#eee1c7}.badge.archived{background:#ead8d5}.score{font-size:13px;line-height:1.65}.arrow{font-size:22px;color:var(--rouge)}.empty{padding:42px;text-align:center;color:var(--muted)}.tabs{display:flex;gap:5px;margin:0 0 18px;overflow:auto}.tab{padding:10px 13px;border-bottom:2px solid transparent;color:var(--muted);white-space:nowrap}.tab.active{border-color:var(--rouge);color:var(--forest);font-weight:700}.detail-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.detail-grid .wide{grid-column:1/-1}dl{display:grid;grid-template-columns:minmax(130px,.75fr) 1.25fr;gap:10px 15px;margin:0}dt{color:var(--muted)}dd{margin:0;overflow-wrap:anywhere}.copy{white-space:pre-wrap;line-height:1.55}.images{display:grid;grid-template-columns:repeat(auto-fill,minmax(190px,1fr));gap:14px}.image{overflow:hidden;border:1px solid #ded5bf;border-radius:7px;background:#fff}.image[draggable=true]{cursor:grab}.image.dragging{opacity:.45}.image img{display:block;width:100%;aspect-ratio:4/5;object-fit:cover;cursor:zoom-in}.image-body{display:grid;gap:9px;padding:12px}.image-body p{margin:0}.image-actions{display:flex;gap:7px;flex-wrap:wrap}.image-actions form{display:inline}.image-actions .button,.image-actions button{min-height:34px;padding:6px 9px;font-size:12px}.upload{display:grid;grid-template-columns:1fr auto;gap:12px;align-items:end;margin:18px 0}.notice{padding:12px 14px;border-radius:6px;background:#dce1c8}.notice.warning{background:#f3dfbf}.notice.error{color:#812c25;background:#f4d8d5}.preview-list{display:flex;gap:8px;flex-wrap:wrap}.preview-list img{width:72px;aspect-ratio:4/5;object-fit:cover;border-radius:4px}.future{border-style:dashed;background:rgba(255,250,240,.48)}dialog{max-width:min(900px,92vw);padding:12px;border:0;border-radius:8px;background:#111}dialog img{display:block;max-width:100%;max-height:82vh}dialog button{position:absolute;right:17px;top:17px} @media(max-width:860px){.filters{grid-template-columns:1fr 1fr}.place-card{grid-template-columns:1fr 1fr}.arrow{display:none}}@media(max-width:620px){main{width:min(100% - 22px,1180px);margin-top:25px}.brand-note{display:none}.filters,.place-card,.detail-grid,.upload{grid-template-columns:1fr}.detail-grid .wide{grid-column:auto}.button,button{width:100%}dl{grid-template-columns:1fr;gap:3px}dd{margin-bottom:8px}}
  </style></head><body><header><a class="brand" href="/admin"><strong class="wordmark">TU<i>U</i>TI</strong><span class="brand-note">PLACES &amp; CONTENT · STAGING</span></a></header><main>${content}</main></body></html>`;
}

function selected(value: string | undefined, candidate: string): string { return value === candidate ? " selected" : ""; }
function tags(values: string[]): string { return values.length ? `<div class="tags">${values.map((value) => `<span class="tag">${escapeHtml(value)}</span>`).join("")}</div>` : `<span class="meta">—</span>`; }
function date(value: string | null): string { return value ? new Intl.DateTimeFormat("en-GB", { dateStyle: "medium" }).format(new Date(value)) : "—"; }
function value(input: unknown): string { return input === null || input === undefined || input === "" ? "—" : escapeHtml(input); }
function boolean(input: boolean | null): string { return input === null ? "Not assessed" : input ? "Yes" : "No"; }

export function renderPlacesAdminList(input: { places: PlaceAdminSummary[]; filters: PlaceAdminFilters; options: PlaceAdminFilterOptions }): string {
  const options = (values: string[], current?: string) => values.map((item) => `<option value="${escapeHtml(item)}"${selected(current, item)}>${escapeHtml(item)}</option>`).join("");
  const cards = input.places.map((place) => `<a class="place-card" href="/admin/places/${encodeURIComponent(place.id)}">
    <div><h2>${escapeHtml(place.name)}</h2><div class="meta">${escapeHtml([place.neighbourhood, place.area, place.region].filter(Boolean).join(" · "))}</div></div>
    <div>${tags(place.categories)}<div class="meta">${escapeHtml(place.subcategories.slice(0, 3).join(" · ") || "No subcategory")}</div></div>
    <div class="score"><span class="badge ${escapeHtml(place.status)}">${escapeHtml(place.status)}</span><br>TUUTI level ${place.offscriptPickLevel} · priority ${place.offscriptPriority}<br>${place.imageCount} ${place.imageCount === 1 ? "image" : "images"}</div><span class="arrow">→</span></a>`).join("");
  return page("Places & Content", `<div class="topbar"><div><h1>Places &amp; Content</h1><p class="meta">${input.places.length} curated ${input.places.length === 1 ? "place" : "places"} in this view.</p></div><a class="button secondary" href="/admin">Back to Operations</a></div>
    <nav class="tabs"><span class="tab active">Places</span><span class="tab">Events · later</span><span class="tab">Stories · later</span></nav>
    <form class="panel filters" method="get" action="/admin/places"><label>Search<input name="search" value="${escapeHtml(input.filters.search ?? "")}" placeholder="Name or location"></label><label>Neighbourhood<select name="neighbourhood"><option value="">All neighbourhoods</option>${options(input.options.neighbourhoods, input.filters.neighbourhood)}</select></label><label>Category<select name="category"><option value="">All categories</option>${options(input.options.categories, input.filters.category)}</select></label><label>Status<select name="status"><option value="">All statuses</option>${options(input.options.statuses, input.filters.status)}</select></label><div class="actions"><button type="submit">Apply filters</button><a class="button secondary" href="/admin/places">Clear</a></div></form>
    ${cards ? `<section class="places">${cards}</section>` : `<section class="panel empty">No places match these filters.</section>`}`);
}

export function renderPlaceAdminDetail(input: { place: PlaceAdminDetail; csrfToken: string; cloudinaryReady: boolean; notice?: { uploaded?: number; removed?: boolean; warning?: string; error?: string } }): string {
  const { place } = input;
  const external = (url: string | null, label: string) => url ? `<a href="${escapeHtml(url)}" target="_blank" rel="noreferrer">${escapeHtml(label)} ↗</a>` : "—";
  const images = place.images.map((image) => `<article class="image" draggable="true" data-image-id="${escapeHtml(image.id)}"><img src="${escapeHtml(image.url)}" alt="${escapeHtml(image.altText ?? place.name)}" loading="lazy" data-preview="${escapeHtml(image.url)}"><div class="image-body"><p><strong>${image.isHeroImage ? "Cover · " : ""}</strong>${escapeHtml(image.originalFilename ?? image.caption ?? image.altText ?? "Legacy photo")}</p><p class="meta">${image.width && image.height ? `${image.width}×${image.height} · ` : ""}${escapeHtml(image.source ?? "legacy")}</p><div class="image-actions">${image.isHeroImage ? "" : `<form method="post" action="/admin/places/${place.id}/photos/${image.id}/cover"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><button class="secondary">Make cover</button></form>`}<form method="post" action="/admin/places/${place.id}/photos/${image.id}/remove" onsubmit="return confirm('Remove this photo from the place? The Cloudinary asset will not be deleted.${image.source !== "dashboard" ? " Field Research may add it again while its source URL remains." : ""}')"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><button class="button danger">Remove link</button></form></div></div></article>`).join("");
  const notices = `${input.notice?.uploaded ? `<p class="notice">${input.notice.uploaded} ${input.notice.uploaded === 1 ? "photo" : "photos"} uploaded.</p>` : ""}${input.notice?.removed ? `<p class="notice">Photo relationship removed. The Cloudinary asset was kept.</p>` : ""}${input.notice?.warning ? `<p class="notice warning">${escapeHtml(input.notice.warning)}</p>` : ""}${input.notice?.error ? `<p class="notice error">${escapeHtml(input.notice.error)}</p>` : ""}`;
  return page(place.name, `<div class="topbar"><div><p class="meta">Places &amp; Content</p><h1>${escapeHtml(place.name)}</h1><p class="meta">${escapeHtml([place.neighbourhood, place.area, place.region].filter(Boolean).join(" · "))}</p></div><a class="button secondary" href="/admin/places">Back to places</a></div>
    <nav class="tabs"><a class="tab active" href="#overview">Overview</a><a class="tab" href="#editorial">Editorial</a><a class="tab" href="#photos">Photos</a><span class="tab">Events · later</span></nav>
    <div class="detail-grid">
      <section class="panel" id="overview"><h2>Overview</h2><dl><dt>Status</dt><dd><span class="badge ${escapeHtml(place.status)}">${escapeHtml(place.status)}</span></dd><dt>Location</dt><dd>${value([place.neighbourhood, place.area, place.region, place.country].filter(Boolean).join(" · "))}</dd><dt>Categories</dt><dd>${tags(place.categories)}</dd><dt>Subcategories</dt><dd>${tags(place.subcategories)}</dd><dt>Vibe</dt><dd>${value(place.vibe)}</dd><dt>Vibe tags</dt><dd>${tags(place.vibeTags)}</dd><dt>Price level</dt><dd>${value(place.priceLevel)}</dd><dt>Opening hours</dt><dd>${value(place.openingHours)}</dd></dl></section>
      <section class="panel" id="editorial"><h2>TUUTI editorial</h2><dl><dt>Pick level</dt><dd>${place.offscriptPickLevel}</dd><dt>Priority</dt><dd>${place.offscriptPriority}</dd><dt>Authenticity</dt><dd>${value(place.authenticity)}</dd><dt>Food orientation</dt><dd>${value(place.foodOrientation)}</dd><dt>Audience orientation</dt><dd>${value(place.audienceOrientation)}</dd><dt>Adventure level</dt><dd>${value(place.adventureLevel)}</dd><dt>Work friendly</dt><dd>${boolean(place.workFriendly)}</dd><dt>Audience</dt><dd>${tags(place.audienceTags)}</dd><dt>Occasions</dt><dd>${tags(place.occasionTags)}</dd></dl></section>
      <section class="panel wide"><h2>Content</h2><h3>Short description</h3><p class="copy">${value(place.shortDescriptionEn ?? place.shortDescriptionFr ?? place.shortDescription)}</p><h3>Practical information</h3><p class="copy">${value(place.practicalInfoEn ?? place.practicalInfoFr ?? place.practicalInfo)}</p><h3>Personal tip</h3><p class="copy">${value(place.personalTipEn ?? place.personalTipFr ?? place.personalTip)}</p><h3>Why TUUTI recommends it</h3><p class="copy">${value(place.offscriptReasonEn ?? place.offscriptReasonFr ?? place.offscriptReasonNl)}</p></section>
      <section class="panel"><h2>Matching</h2><dl><dt>Best for</dt><dd>${tags(place.bestFor)}</dd><dt>Not ideal for</dt><dd>${tags(place.notIdealFor)}</dd><dt>Traveller types</dt><dd>${tags(place.travellerTypes)}</dd><dt>Best timing</dt><dd>${tags(place.bestTiming)}</dd><dt>Amenities</dt><dd>${tags(place.amenities)}</dd></dl></section>
      <section class="panel"><h2>Links &amp; verification</h2><dl><dt>Google Maps</dt><dd>${external(place.googleMapsUrl, "Open map")}</dd><dt>Instagram</dt><dd>${external(place.instagramUrl, "Open Instagram")}</dd><dt>Facebook</dt><dd>${external(place.facebookUrl, "Open Facebook")}</dd><dt>TikTok</dt><dd>${external(place.tiktokUrl, "Open TikTok")}</dd><dt>Coordinates</dt><dd>${place.latitude !== null && place.longitude !== null ? `${place.latitude}, ${place.longitude}` : "—"}</dd><dt>Source</dt><dd>${value(place.source)}</dd><dt>Last verified</dt><dd>${date(place.lastVerifiedAt)}</dd><dt>Updated</dt><dd>${date(place.updatedAt)}</dd></dl></section>
      <section class="panel wide" id="photos"><h2>Photos <span class="meta">${place.images.length} directly attached</span></h2>${notices}<p class="meta">JPEG · portrait 4:5. Multiple files are supported. Drag thumbnails to change their order.</p>${input.cloudinaryReady ? `<form class="upload" method="post" enctype="multipart/form-data" action="/admin/places/${place.id}/photos"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><label>Add photos<input id="photo-files" type="file" name="photos" accept="image/jpeg,.jpg,.jpeg" multiple required><span class="meta" id="ratio-warning"></span><span class="preview-list" id="upload-previews"></span></label><button>Upload photos</button></form>` : `<p class="notice warning">Cloudinary uploads are unavailable until the three staging environment variables are configured.</p>`}${images ? `<div class="images" id="photo-grid">${images}</div><p class="meta" id="order-status"></p>` : `<p class="meta">No direct place images are attached yet.</p>`}</section>
      <section class="panel wide future"><h2>Events</h2><p class="meta">This place is ready for a future Events tab. Event ingestion, AI extraction and approval are intentionally not part of this release.</p></section>
    </div><dialog id="photo-dialog"><button type="button" aria-label="Close">Close</button><img alt="Photo preview"></dialog><script>
    const csrf=${JSON.stringify(input.csrfToken)},placeId=${JSON.stringify(place.id)},grid=document.querySelector('#photo-grid'),status=document.querySelector('#order-status');let dragged;
    if(grid){grid.addEventListener('dragstart',e=>{dragged=e.target.closest('[data-image-id]');if(dragged)dragged.classList.add('dragging')});grid.addEventListener('dragend',async()=>{if(!dragged)return;dragged.classList.remove('dragging');dragged=undefined;const image_ids=[...grid.querySelectorAll('[data-image-id]')].map(x=>x.dataset.imageId);status.textContent='Saving order…';const response=await fetch('/admin/places/'+placeId+'/photos/order',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({_csrf:csrf,image_ids})});status.textContent=response.ok?'Order saved.':'Could not save the new order.'});grid.addEventListener('dragover',e=>{e.preventDefault();const target=e.target.closest('[data-image-id]');if(dragged&&target&&target!==dragged){const box=target.getBoundingClientRect();grid.insertBefore(dragged,e.clientY<box.top+box.height/2?target:target.nextSibling)}})}
    const dialog=document.querySelector('#photo-dialog');document.addEventListener('click',e=>{const image=e.target.closest('[data-preview]');if(image){dialog.querySelector('img').src=image.dataset.preview;dialog.showModal()}});dialog.querySelector('button').addEventListener('click',()=>dialog.close());
    const files=document.querySelector('#photo-files'),previews=document.querySelector('#upload-previews'),warning=document.querySelector('#ratio-warning');if(files)files.addEventListener('change',()=>{previews.innerHTML='';warning.textContent='';[...files.files].forEach(file=>{const url=URL.createObjectURL(file),img=new Image();img.onload=()=>{if(Math.abs(img.naturalWidth/img.naturalHeight-.8)>.04)warning.textContent+=' '+file.name+' is not approximately 4:5.'};img.src=url;const thumb=document.createElement('img');thumb.src=url;thumb.alt=file.name;previews.append(thumb)})});
    </script>`);
}
