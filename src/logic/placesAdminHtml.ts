import type { PlaceAdminDetail, PlaceAdminFilterOptions, PlaceAdminFilters, PlaceAdminSummary } from "../data/placesAdminRepository.js";
import { escapeHtml } from "./sourcesAdminHtml.js";

function page(title: string, content: string): string {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#17201d"><meta name="apple-mobile-web-app-capable" content="yes"><meta name="apple-mobile-web-app-title" content="TUUTI"><link rel="manifest" href="/admin-assets/manifest.webmanifest?v=4"><link rel="apple-touch-icon" href="/apple-touch-icon.png?v=4"><link rel="icon" type="image/png" href="/admin-assets/tuuti_dashboard_flavicon.png"><title>${escapeHtml(title)} · TUUTI</title><style>
  :root{font-family:Inter,ui-sans-serif,system-ui,sans-serif;color:#102a24;background:#f2ebd9;--forest:#102a24;--rouge:#d95b32;--sand:#f2ebd9;--paper:#fffaf0;--muted:#68705a}*{box-sizing:border-box}body{margin:0;min-width:320px;background:radial-gradient(circle at 90% 0,rgba(217,91,50,.08),transparent 28rem),var(--sand)}a{color:inherit}header{padding:18px max(18px,calc((100% - 1180px)/2));border-bottom:3px solid var(--rouge);color:var(--sand);background:var(--forest)}header a{text-decoration:none}.admin-header{display:flex;align-items:center;gap:12px}.brand{display:flex;flex:1;align-items:baseline;justify-content:space-between;gap:20px}.mobile-back{display:none;min-height:38px;padding:6px 11px;border-color:rgba(242,235,217,.45);color:var(--sand);background:transparent;font-size:22px;line-height:1}.wordmark{font-size:24px;font-weight:760;letter-spacing:.3em}.wordmark i{color:var(--rouge);font-style:normal}.brand-note{color:#d9d2bd;font-size:12px;letter-spacing:.06em}main{width:min(1180px,calc(100% - 32px));margin:38px auto 60px}h1,h2,h3{font-family:Canela,Georgia,serif;font-weight:500}h1{margin:0;font-size:clamp(34px,5vw,50px);line-height:1.05}h2{margin:0 0 14px;font-size:24px}h3{margin:0 0 7px;font-size:19px}.topbar,.actions{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap}.topbar{margin-bottom:24px}.meta{color:var(--muted);font-size:14px;line-height:1.5}.button,button{display:inline-flex;min-height:42px;align-items:center;justify-content:center;padding:9px 15px;border:1px solid var(--forest);border-radius:5px;color:var(--sand);background:var(--forest);font:inherit;font-weight:680;text-decoration:none;cursor:pointer}.button.secondary,button.secondary{color:var(--forest);background:transparent}.button.danger{border-color:#9b3d35;color:#9b3d35;background:transparent}.button:hover,button:hover{border-color:var(--rouge);background:var(--rouge);color:white}.panel,.place-card{border:1px solid rgba(16,42,36,.18);border-radius:8px;background:rgba(255,250,240,.94);box-shadow:0 8px 24px rgba(16,42,36,.05)}.panel{padding:24px;margin-bottom:18px}.filters{display:grid;grid-template-columns:2fr repeat(3,1fr);gap:14px;align-items:end}label{display:grid;gap:7px;color:var(--forest);font-size:12px;font-weight:720;letter-spacing:.025em;text-transform:uppercase}input,select{width:100%;min-height:42px;padding:9px 11px;border:1px solid rgba(16,42,36,.3);border-radius:4px;background:#fffdf7;font:inherit}.places{display:grid;gap:12px}.place-card{display:grid;grid-template-columns:minmax(220px,1.3fr) minmax(180px,1fr) minmax(170px,.8fr) auto;gap:20px;align-items:center;padding:19px;text-decoration:none}.place-card:hover{border-color:var(--rouge);transform:translateY(-1px)}.place-card h2{margin:0 0 5px;font-size:21px}.tags{display:flex;gap:6px;flex-wrap:wrap}.tag,.badge{display:inline-flex;padding:4px 8px;border-radius:999px;font-size:12px}.tag{background:#e9e2cf}.badge{font-weight:750;text-transform:capitalize;background:#dce1c8}.badge.draft{background:#eee1c7}.badge.archived{background:#ead8d5}.score{font-size:13px;line-height:1.65}.arrow{font-size:22px;color:var(--rouge)}.empty{padding:42px;text-align:center;color:var(--muted)}.tabs{display:flex;gap:5px;margin:0 0 18px;overflow:auto}.tab{padding:10px 13px;border-bottom:2px solid transparent;color:var(--muted);white-space:nowrap}.tab.active{border-color:var(--rouge);color:var(--forest);font-weight:700}.detail-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.detail-grid .wide{grid-column:1/-1}dl{display:grid;grid-template-columns:minmax(130px,.75fr) 1.25fr;gap:10px 15px;margin:0}dt{color:var(--muted)}dd{margin:0;overflow-wrap:anywhere}.copy{white-space:pre-wrap;line-height:1.55}.images{display:grid;grid-template-columns:repeat(auto-fill,minmax(190px,1fr));gap:14px}.image{position:relative;overflow:hidden;border:1px solid #ded5bf;border-radius:7px;background:#fff}.image[draggable=true]{cursor:grab}.image.dragging{opacity:.45}.image img{display:block;width:100%;aspect-ratio:4/5;object-fit:cover;cursor:zoom-in}.photo-rank{display:none;position:absolute;z-index:1;top:10px;left:10px;padding:6px 9px;border-radius:999px;color:var(--sand);background:rgba(16,42,36,.92);box-shadow:0 2px 8px rgba(0,0,0,.2);font-size:12px;font-weight:800;letter-spacing:.02em}.photo-rank.active{display:inline-flex}.photo-rank.cover{background:var(--rouge)}.image-body{display:grid;gap:9px;padding:12px}.image-body p{margin:0}.image-actions{display:flex;gap:7px;flex-wrap:wrap}.image-actions form{display:inline}.image-actions .button,.image-actions button{min-height:34px;padding:6px 9px;font-size:12px}.upload{display:grid;grid-template-columns:1fr auto;gap:12px;align-items:end;margin:18px 0}.notice{padding:12px 14px;border-radius:6px;background:#dce1c8}.notice.warning{background:#f3dfbf}.notice.error{color:#812c25;background:#f4d8d5}.preview-list{display:flex;gap:8px;flex-wrap:wrap}.preview-list img{width:72px;aspect-ratio:4/5;object-fit:cover;border-radius:4px}.video-admin{display:grid;grid-template-columns:minmax(220px,360px) 1fr;gap:22px;align-items:start}.video-admin video,.video-preview{display:block;width:100%;max-height:560px;border-radius:8px;background:#111}.video-details{display:grid;gap:13px}.video-preview[hidden]{display:none}.future{border-style:dashed;background:rgba(255,250,240,.48)}dialog{max-width:min(900px,92vw);padding:12px;border:0;border-radius:8px;background:#111}dialog img{display:block;max-width:100%;max-height:82vh}dialog button{position:absolute;right:17px;top:17px} @media(max-width:860px){.filters{grid-template-columns:1fr 1fr}.place-card{grid-template-columns:1fr 1fr}.video-admin{grid-template-columns:1fr}.arrow{display:none}}@media(max-width:620px){main{width:min(100% - 22px,1180px);margin-top:25px}.brand-note{display:none}.mobile-back{display:inline-flex;width:42px;flex:0 0 42px}.filters,.place-card,.detail-grid,.upload{grid-template-columns:1fr}.detail-grid .wide{grid-column:auto}.button,button{width:100%}dl{grid-template-columns:1fr;gap:3px}dd{margin-bottom:8px}}
  </style></head><body><header><div class="admin-header"><button class="mobile-back" type="button" aria-label="Go back" onclick="if(history.length>1){history.back()}else{location.href='/admin'}">←</button><a class="brand" href="/admin"><strong class="wordmark">TU<i>U</i>TI</strong><span class="brand-note">PLACES &amp; CONTENT · STAGING</span></a></div></header><main>${content}</main></body></html>`;
}

function selected(value: string | undefined, candidate: string): string { return value === candidate ? " selected" : ""; }
function tags(values: string[] = []): string { return values.length ? `<div class="tags">${values.map((value) => `<span class="tag">${escapeHtml(value)}</span>`).join("")}</div>` : `<span class="meta">—</span>`; }
function date(value: string | null): string { return value ? new Intl.DateTimeFormat("en-GB", { dateStyle: "medium" }).format(new Date(value)) : "—"; }
function value(input: unknown): string { return input === null || input === undefined || input === "" ? "—" : escapeHtml(input); }
function boolean(input: boolean | null): string { return input === null ? "Not assessed" : input ? "Yes" : "No"; }

export function renderPlacesAdminList(input: { places: PlaceAdminSummary[]; filters: PlaceAdminFilters; options: PlaceAdminFilterOptions }): string {
  const options = (values: string[], current?: string) => values.map((item) => `<option value="${escapeHtml(item)}"${selected(current, item)}>${escapeHtml(item)}</option>`).join("");
  const cards = input.places.map((place) => `<a class="place-card" href="/admin/places/${encodeURIComponent(place.id)}">
    <div><h2>${escapeHtml(place.name)}</h2><div class="meta">${escapeHtml([place.neighbourhood, place.area, place.region].filter(Boolean).join(" · "))}</div></div>
    <div>${tags(place.categories)}<div class="meta">${escapeHtml(place.subcategories.slice(0, 3).join(" · ") || "No subcategory")}</div></div>
    <div class="score"><span class="badge ${escapeHtml(place.status)}">${escapeHtml(place.status)}</span><br>TUUTI level ${place.offscriptPickLevel} · priority ${place.offscriptPriority}<br>${place.imageCount} ${place.imageCount === 1 ? "image" : "images"}</div><span class="arrow">→</span></a>`).join("");
  return page("Places & Content", `<div class="topbar"><div><h1>Places &amp; Content</h1><p class="meta">${input.places.length} curated ${input.places.length === 1 ? "place" : "places"} in this view.</p></div><a class="button secondary" href="/admin">Back to Operations</a></div>
    <nav class="tabs"><span class="tab active">Places</span><a class="tab" href="/admin/events">Events</a><span class="tab">Stories · later</span></nav>
    <form class="panel filters" method="get" action="/admin/places"><label>Search<input name="search" value="${escapeHtml(input.filters.search ?? "")}" placeholder="Name or location"></label><label>Neighbourhood<select name="neighbourhood"><option value="">All neighbourhoods</option>${options(input.options.neighbourhoods, input.filters.neighbourhood)}</select></label><label>Category<select name="category"><option value="">All categories</option>${options(input.options.categories, input.filters.category)}</select></label><label>Status<select name="status"><option value="">All statuses</option>${options(input.options.statuses, input.filters.status)}</select></label><div class="actions"><button type="submit">Apply filters</button><a class="button secondary" href="/admin/places">Clear</a></div></form>
    ${cards ? `<section class="places">${cards}</section>` : `<section class="panel empty">No places match these filters.</section>`}`);
}

type PlaceAdminNotice = { uploaded?: number; removed?: boolean; warning?: string; error?: string; videoUploaded?: boolean; videoReplaced?: boolean; videoRemoved?: boolean; videoWarning?: string; videoError?: string; editorialSaved?: boolean; editorialChanged?: number; editorialError?: string; unlocked?: string; archived?: boolean; restored?: boolean; dishSaved?: boolean; dishRemoved?: boolean; dishError?: string };

export function renderPlaceAdminDetail(input: { place: PlaceAdminDetail; csrfToken: string; cloudinaryReady: boolean; edit?: boolean; notice?: PlaceAdminNotice }): string {
  const { place } = input;
  const locked = new Set(place.editorialLockedFields);
  const lock = (field: string) => locked.has(field) ? `<span class="lock">🔒 Editorial</span>` : "";
  const field = (name: string, label: string, current: unknown, options?: { wide?: boolean; textarea?: boolean; type?: string }) => `<label class="${options?.wide ? "wide" : ""}"><span class="field-title">${escapeHtml(label)}${lock(name)}</span>${options?.textarea ? `<textarea name="${name}">${escapeHtml(Array.isArray(current) ? current.join(", ") : current ?? "")}</textarea>` : `<input name="${name}" type="${options?.type ?? "text"}" value="${escapeHtml(Array.isArray(current) ? current.join(", ") : current ?? "")}">`}</label>`;
  const selectField = (name: string, label: string, current: unknown, choices: Array<[string, string]>) => `<label><span class="field-title">${escapeHtml(label)}${lock(name)}</span><select name="${name}">${choices.map(([value, text]) => `<option value="${escapeHtml(value)}"${String(current ?? "") === value ? " selected" : ""}>${escapeHtml(text)}</option>`).join("")}</select></label>`;
  const editor = `<form method="post" action="/admin/places/${place.id}/editorial"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><div class="editor">
    ${field("name", "Name", place.name)}${field("neighbourhood", "Neighbourhood", place.neighbourhood)}${field("area", "Area", place.area)}${field("categories", "Categories · comma separated", place.categories)}${field("subcategories", "Subcategories · comma separated", place.subcategories)}
    ${field("short_description_en", "Short description · English", place.shortDescriptionEn, { wide: true, textarea: true })}${field("short_description_fr", "Short description · French", place.shortDescriptionFr, { wide: true, textarea: true })}
    ${field("practical_info_en", "Practical information · English", place.practicalInfoEn, { wide: true, textarea: true })}${field("practical_info_fr", "Practical information · French", place.practicalInfoFr, { wide: true, textarea: true })}
    ${field("personal_tip_en", "Personal tip · English", place.personalTipEn, { wide: true, textarea: true })}${field("personal_tip_fr", "Personal tip · French", place.personalTipFr, { wide: true, textarea: true })}
    ${field("opening_hours", "Opening hours", place.openingHours, { wide: true, textarea: true })}
    ${field("price_level", "Price level · 1–5", place.priceLevel, { type: "number" })}${field("vibe", "Vibe", place.vibe)}${field("vibe_tags", "Vibe tags · comma separated", place.vibeTags)}${field("amenities", "Amenities · comma separated", place.amenities)}
    ${field("reservation_phone", "Phone number · include country code (e.g. +221771234567)", place.reservationPhone, { type: "tel" })}${field("instagram_url", "Instagram URL", place.instagramUrl)}${field("facebook_url", "Facebook URL", place.facebookUrl)}${field("tiktok_url", "TikTok URL", place.tiktokUrl)}${field("google_maps_url", "Google Maps URL", place.googleMapsUrl)}
    ${field("offscript_pick_level", "TUUTI pick level · 0–3", place.offscriptPickLevel, { type: "number" })}${field("offscript_priority", "TUUTI priority · 0–100", place.offscriptPriority, { type: "number" })}
    ${field("offscript_reason_nl", "Why TUUTI recommends it · Dutch", place.offscriptReasonNl, { wide: true, textarea: true })}${field("offscript_reason_en", "Why TUUTI recommends it · English", place.offscriptReasonEn, { wide: true, textarea: true })}${field("offscript_reason_fr", "Why TUUTI recommends it · French", place.offscriptReasonFr, { wide: true, textarea: true })}
    ${field("authenticity", "Authenticity · 0–4", place.authenticity, { type: "number" })}${field("food_orientation", "Food orientation · −2 to 2", place.foodOrientation, { type: "number" })}${field("audience_orientation", "Audience orientation · −2 to 2", place.audienceOrientation, { type: "number" })}${field("adventure_level", "Adventure level · 0–3", place.adventureLevel, { type: "number" })}
    ${field("audience_tags", "Audience tags · comma separated", place.audienceTags)}${field("occasion_tags", "Occasion tags · comma separated", place.occasionTags)}${field("dietary_tags", "Dietary tags · comma separated", place.dietaryTags)}
    ${selectField("work_friendly", "Work friendly", place.workFriendly === null ? "" : String(place.workFriendly), [["", "Not assessed"], ["true", "Yes"], ["false", "No"]])}${selectField("status", "Status", place.status, [["draft", "Draft"], ["ready", "Ready"], ["premium", "Premium"]])}
  </div><div class="actions"><button type="submit">Save editorial changes</button><a class="button secondary" href="/admin/places/${place.id}#editorial">Cancel</a></div></form>`;
  const unlocks = place.editorialLockedFields.length ? `<div class="locked-fields"><h3>Manually managed fields</h3><p class="meta">Unlocking keeps the current value. A later sync may update it again.</p>${place.editorialLockedFields.map((name) => `<div class="unlock-row"><span>${escapeHtml(name.replaceAll("_", " "))}</span><form method="post" action="/admin/places/${place.id}/editorial/unlock"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><input type="hidden" name="field" value="${escapeHtml(name)}"><button class="secondary">Use Field Research again</button></form></div>`).join("")}</div>` : `<p class="meta">All fields currently follow the normal sync workflow.</p>`;
  const editorialNotices = `${input.notice?.editorialSaved ? `<p class="notice">Saved. ${input.notice.editorialChanged ? `${input.notice.editorialChanged} changed ${input.notice.editorialChanged === 1 ? "field is" : "fields are"} now manually managed.` : "No values changed, so no fields were locked."}</p>` : ""}${input.notice?.editorialError ? `<p class="notice error">${escapeHtml(input.notice.editorialError)}</p>` : ""}${input.notice?.unlocked ? `<p class="notice">${escapeHtml(input.notice.unlocked.replaceAll("_", " "))} can be managed by Field Research again. Its current value was kept.</p>` : ""}${input.notice?.archived ? `<p class="notice warning">This place is archived and no longer eligible for recommendations.</p>` : ""}${input.notice?.restored ? `<p class="notice">This place was restored.</p>` : ""}`;
  const external = (url: string | null, label: string) => url ? `<a href="${escapeHtml(url)}" target="_blank" rel="noreferrer">${escapeHtml(label)} ↗</a>` : "—";
  const images = place.images.map((image) => `<article class="image" draggable="true" data-image-id="${escapeHtml(image.id)}" data-cover="${image.isHeroImage}"><span class="photo-rank" aria-live="polite"></span><img src="${escapeHtml(image.url)}" alt="${escapeHtml(image.altText ?? place.name)}" loading="lazy" data-preview="${escapeHtml(image.url)}"><div class="image-body"><p><strong>${image.isHeroImage ? "Cover · " : ""}</strong>${escapeHtml(image.originalFilename ?? image.caption ?? image.altText ?? "Legacy photo")}</p><p class="meta">${image.width && image.height ? `${image.width}×${image.height} · ` : ""}${escapeHtml(image.source ?? "legacy")}</p><div class="image-actions">${image.isHeroImage ? "" : `<form method="post" action="/admin/places/${place.id}/photos/${image.id}/cover"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><button class="secondary">Make cover</button></form>`}<form method="post" action="/admin/places/${place.id}/photos/${image.id}/remove" onsubmit="return confirm('Remove this photo from the place? The Cloudinary asset will not be deleted.${image.source !== "dashboard" ? " Field Research may add it again while its source URL remains." : ""}')"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><button class="button danger">Remove link</button></form></div></div></article>`).join("");
  const notices = `${input.notice?.uploaded ? `<p class="notice">${input.notice.uploaded} ${input.notice.uploaded === 1 ? "photo" : "photos"} uploaded.</p>` : ""}${input.notice?.removed ? `<p class="notice">Photo relationship removed. The Cloudinary asset was kept.</p>` : ""}${input.notice?.warning ? `<p class="notice warning">${escapeHtml(input.notice.warning)}</p>` : ""}${input.notice?.error ? `<p class="notice error">${escapeHtml(input.notice.error)}</p>` : ""}`;
  const videoNotices = `${input.notice?.videoUploaded ? `<p class="notice">Video ${input.notice.videoReplaced ? "replaced" : "uploaded"}. ${input.notice.videoReplaced ? "The previous Cloudinary asset was kept." : ""}</p>` : ""}${input.notice?.videoRemoved ? `<p class="notice">Video relationship removed. The Cloudinary asset was kept.</p>` : ""}${input.notice?.videoWarning ? `<p class="notice warning">${escapeHtml(input.notice.videoWarning)}</p>` : ""}${input.notice?.videoError ? `<p class="notice error">${escapeHtml(input.notice.videoError)}</p>` : ""}`;
  const video = place.video;
  const videoContent = video ? `<div class="video-admin"><video controls preload="metadata" poster="${escapeHtml(video.posterUrl)}" src="${escapeHtml(video.url)}"></video><div class="video-details"><h3>${escapeHtml(video.originalFilename)}</h3><p class="meta">${video.width}×${video.height} · ${video.durationSeconds.toFixed(1)} seconds · ${escapeHtml(video.format.toUpperCase())} · ${(video.fileSizeBytes / 1024 / 1024).toFixed(1)} MB</p><p class="meta">Removing or replacing this link never permanently deletes its Cloudinary asset.</p><form method="post" action="/admin/places/${place.id}/video/${video.id}/remove" onsubmit="return confirm('Remove this video from the place? The Cloudinary asset will be kept.')"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><button class="button danger">Remove video link</button></form></div></div>` : `<p class="meta">No active video is linked to this place yet.</p>`;
  const ratingLabel: Record<string, string> = { loved: "Loved it", okay: "It was okay", disliked: "Did not like it", did_not_go: "Did not go" };
  const feedback = place.feedback.map((item) => `<article class="feedback-item"><div class="topbar"><strong>${escapeHtml(ratingLabel[item.rating] ?? item.rating)}</strong><span class="meta">${date(item.createdAt)}</span></div>${item.positiveDetail ? `<p class="copy">${escapeHtml(item.positiveDetail)}</p>` : ""}${item.freeText ? `<p class="copy">${escapeHtml(item.freeText)}</p>` : ""}${item.reason ? `<p class="meta">Reason: ${escapeHtml(item.reason.replaceAll("_", " "))}</p>` : ""}${item.travellerType || item.requestedVibe ? `<p class="meta">${escapeHtml([item.travellerType, item.requestedVibe].filter(Boolean).join(" · "))}</p>` : ""}</article>`).join("");
  const availabilityLabels: Record<string, string> = { known_for: "Known for", usually_available: "Usually available", sometimes_available: "Sometimes available" };
  const dishes = (place.dishes ?? []).map((dish) => `<article class="dish-item"><div><strong>${escapeHtml(dish.name)}</strong><p class="meta">${escapeHtml(availabilityLabels[dish.availabilityStatus] ?? dish.availabilityStatus)} · verified ${date(dish.lastVerifiedAt)} · ${escapeHtml(dish.source)}</p></div><form class="dish-form dish-edit" method="post" action="/admin/places/${place.id}/dishes"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><input type="hidden" name="dish_id" value="${escapeHtml(dish.dishId)}"><label>Availability<select name="availability_status"><option value="known_for"${dish.availabilityStatus === "known_for" ? " selected" : ""}>Known for</option><option value="usually_available"${dish.availabilityStatus === "usually_available" ? " selected" : ""}>Usually available</option><option value="sometimes_available"${dish.availabilityStatus === "sometimes_available" ? " selected" : ""}>Sometimes available</option></select></label><label>Last verified<input name="last_verified_at" type="date" value="${dish.lastVerifiedAt ? escapeHtml(dish.lastVerifiedAt.slice(0, 10)) : ""}"></label><label>Notes<input name="notes" value="${escapeHtml(dish.notes ?? "")}"></label><button>Save</button></form><form method="post" action="/admin/places/${place.id}/dishes/${dish.id}/remove" onsubmit="return confirm('Remove this dish from this place?')"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><button class="button danger">Remove</button></form></article>`).join("");
  const existingDishIds = new Set((place.dishes ?? []).map((dish) => dish.dishId));
  const dishOptions = (place.dishCatalog ?? []).filter((dish) => !existingDishIds.has(dish.id)).map((dish) => `<option value="${escapeHtml(dish.id)}">${escapeHtml(dish.name)}</option>`).join("");
  const dishNotices = `${input.notice?.dishSaved ? `<p class="notice">Dish information saved.</p>` : ""}${input.notice?.dishRemoved ? `<p class="notice">Dish removed from this place.</p>` : ""}${input.notice?.dishError ? `<p class="notice error">${escapeHtml(input.notice.dishError)}</p>` : ""}`;
  return page(place.name, `<div class="topbar"><div><p class="meta">Places &amp; Content</p><h1>${escapeHtml(place.name)}</h1><p class="meta">${escapeHtml([place.neighbourhood, place.area, place.region].filter(Boolean).join(" · "))}</p></div><a class="button secondary" href="/admin/places">Back to places</a></div>
    <nav class="tabs"><a class="tab active" href="#overview">Overview</a><a class="tab" href="#editorial">Editorial</a><a class="tab" href="#dishes">Dishes</a><a class="tab" href="#feedback">Feedback</a><a class="tab" href="#photos">Photos</a><a class="tab" href="#videos">Videos</a><a class="tab" href="/admin/events">Events</a></nav>
    <div class="detail-grid">
      <section class="panel" id="overview"><h2>Overview</h2><dl><dt>Status</dt><dd><span class="badge ${escapeHtml(place.status)}">${escapeHtml(place.status)}</span></dd><dt>Location</dt><dd>${value([place.neighbourhood, place.area, place.region, place.country].filter(Boolean).join(" · "))}</dd><dt>Categories</dt><dd>${tags(place.categories)}</dd><dt>Subcategories</dt><dd>${tags(place.subcategories)}</dd><dt>Vibe</dt><dd>${value(place.vibe)}</dd><dt>Vibe tags</dt><dd>${tags(place.vibeTags)}</dd><dt>Price level</dt><dd>${value(place.priceLevel)}</dd><dt>Phone number</dt><dd>${value(place.reservationPhone)}</dd><dt>Opening hours</dt><dd>${value(place.openingHours)}</dd></dl></section>
      <section class="panel wide" id="editorial"><style>textarea{width:100%;min-height:105px;padding:9px 11px;border:1px solid rgba(16,42,36,.3);border-radius:4px;background:#fffdf7;font:inherit;resize:vertical}.editor{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:17px}.editor .wide{grid-column:1/-1}.field-title{display:flex;align-items:center;justify-content:space-between;gap:8px}.lock{padding:3px 7px;border-radius:999px;color:#713126;background:#f1d4c8;font-size:10px;letter-spacing:0;text-transform:none}.locked-fields{display:grid;gap:8px;margin-top:18px}.unlock-row{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:9px 0;border-top:1px solid rgba(16,42,36,.12)}.unlock-row form{margin:0}.unlock-row button{min-height:34px;padding:6px 9px;font-size:12px}.danger-zone{margin-top:22px;padding-top:18px;border-top:1px solid rgba(155,61,53,.35)}@media(max-width:620px){.editor{grid-template-columns:1fr}.editor .wide{grid-column:auto}.unlock-row{align-items:stretch;flex-direction:column}}</style><div class="topbar"><div><h2>TUUTI editorial</h2><p class="meta">Only values you actually change are protected from future syncs.</p></div>${input.edit ? "" : `<a class="button" href="/admin/places/${place.id}?edit=1#editorial">Edit</a>`}</div>${editorialNotices}${input.edit ? editor : `<dl><dt>Pick level</dt><dd>${place.offscriptPickLevel}</dd><dt>Priority</dt><dd>${place.offscriptPriority}</dd><dt>Authenticity</dt><dd>${value(place.authenticity)}</dd><dt>Food orientation</dt><dd>${value(place.foodOrientation)}</dd><dt>Audience orientation</dt><dd>${value(place.audienceOrientation)}</dd><dt>Adventure level</dt><dd>${value(place.adventureLevel)}</dd><dt>Work friendly</dt><dd>${boolean(place.workFriendly)}</dd><dt>Audience</dt><dd>${tags(place.audienceTags)}</dd><dt>Occasions</dt><dd>${tags(place.occasionTags)}</dd><dt>Dietary options</dt><dd>${tags(place.dietaryTags)}</dd></dl>`}${unlocks}<p class="meta">Last editorial update: ${date(place.editorialUpdatedAt)}${place.editorialUpdatedBy ? ` · ${escapeHtml(place.editorialUpdatedBy)}` : ""}</p><div class="danger-zone"><h3>${place.status === "archived" ? "Restore place" : "Archive place"}</h3><p class="meta">${place.status === "archived" ? `Restore this place to ${escapeHtml(place.statusBeforeArchive ?? "draft")}.` : "Archiving removes this place from recommendations while preserving its content, photos and history."}</p><form method="post" action="/admin/places/${place.id}/${place.status === "archived" ? "restore" : "archive"}"${place.status === "archived" ? "" : ` onsubmit="return confirm('Archive this place? It will no longer be recommended, but can be restored later.')"`}><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><button class="${place.status === "archived" ? "secondary" : "danger"}">${place.status === "archived" ? "Restore place" : "Archive place"}</button></form></div></section>
      <section class="panel wide"><h2>Content</h2><h3>Short description</h3><p class="copy">${value(place.shortDescriptionEn ?? place.shortDescriptionFr ?? place.shortDescription)}</p><h3>Practical information</h3><p class="copy">${value(place.practicalInfoEn ?? place.practicalInfoFr ?? place.practicalInfo)}</p><h3>Personal tip</h3><p class="copy">${value(place.personalTipEn ?? place.personalTipFr ?? place.personalTip)}</p><h3>Why TUUTI recommends it</h3><p class="copy">${value(place.offscriptReasonEn ?? place.offscriptReasonFr ?? place.offscriptReasonNl)}</p></section>
      <section class="panel"><h2>Matching</h2><dl><dt>Best for</dt><dd>${tags(place.bestFor)}</dd><dt>Not ideal for</dt><dd>${tags(place.notIdealFor)}</dd><dt>Traveller types</dt><dd>${tags(place.travellerTypes)}</dd><dt>Best timing</dt><dd>${tags(place.bestTiming)}</dd><dt>Amenities</dt><dd>${tags(place.amenities)}</dd></dl></section>
      <section class="panel"><h2>Links &amp; verification</h2><dl><dt>Google Maps</dt><dd>${external(place.googleMapsUrl, "Open map")}</dd><dt>Instagram</dt><dd>${external(place.instagramUrl, "Open Instagram")}</dd><dt>Facebook</dt><dd>${external(place.facebookUrl, "Open Facebook")}</dd><dt>TikTok</dt><dd>${external(place.tiktokUrl, "Open TikTok")}</dd><dt>Coordinates</dt><dd>${place.latitude !== null && place.longitude !== null ? `${place.latitude}, ${place.longitude}` : "—"}</dd><dt>Source</dt><dd>${value(place.source)}</dd><dt>Last verified</dt><dd>${date(place.lastVerifiedAt)}</dd><dt>Updated</dt><dd>${date(place.updatedAt)}</dd></dl></section>
      <section class="panel wide" id="dishes"><style>.dish-list{display:grid;gap:10px;margin:18px 0}.dish-item{display:grid;grid-template-columns:minmax(140px,.7fr) minmax(460px,2fr) auto;align-items:end;gap:14px;padding:14px;border:1px solid rgba(16,42,36,.15);border-radius:6px;background:#fffdf7}.dish-item p{margin:5px 0}.dish-form{display:grid;grid-template-columns:1fr 1fr 1fr 2fr auto;gap:10px;align-items:end}.dish-edit{grid-template-columns:1fr 1fr 1.5fr auto}@media(max-width:980px){.dish-item{grid-template-columns:1fr}.dish-form,.dish-edit{grid-template-columns:1fr 1fr}}@media(max-width:620px){.dish-form,.dish-edit{grid-template-columns:1fr}.dish-item button{width:100%}}</style><h2>Dishes</h2><p class="meta">Editorial knowledge, not a promise that a dish is available today. TUUTI communicates this certainty level to travellers.</p>${dishNotices}${dishes ? `<div class="dish-list">${dishes}</div>` : `<p class="meta">No dishes have been linked to this place yet.</p>`}${dishOptions ? `<form class="dish-form" method="post" action="/admin/places/${place.id}/dishes"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><label>Dish<select name="dish_id" required><option value="">Choose a dish</option>${dishOptions}</select></label><label>Availability<select name="availability_status" required><option value="known_for">Known for</option><option value="usually_available">Usually available</option><option value="sometimes_available">Sometimes available</option></select></label><label>Last verified<input name="last_verified_at" type="date"></label><label>Notes<input name="notes" placeholder="Optional context"></label><button>Add dish</button></form>` : `<p class="meta">All catalogued dishes are already linked.</p>`}</section>
      <section class="panel wide" id="feedback"><style>.feedback-list{display:grid;gap:12px}.feedback-item{padding:15px;border:1px solid rgba(16,42,36,.15);border-radius:6px;background:#fffdf7}.feedback-item .topbar{margin-bottom:7px}.feedback-item p{margin:6px 0}</style><h2>Feedback <span class="meta">${place.feedback.length} ${place.feedback.length === 1 ? "response" : "responses"}</span></h2><p class="meta">Feedback collected through TUUTI for this place. Telephone numbers are deliberately not shown.</p>${feedback ? `<div class="feedback-list">${feedback}</div>` : `<p class="meta">No feedback has been received for this place yet.</p>`}</section>
      <section class="panel wide" id="photos"><h2>Photos <span class="meta">${place.images.length} directly attached</span></h2>${notices}<p class="meta">JPEG · portrait 4:5. New uploads are stored by Cloudinary at 1200 × 1500 px. Multiple files are supported. Drag thumbnails to change their order. TUUTI 1 is always the cover; TUUTI 2 and 3 follow the order of the other photos.</p>${input.cloudinaryReady ? `<form class="upload" method="post" enctype="multipart/form-data" action="/admin/places/${place.id}/photos"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><label>Add photos<input id="photo-files" type="file" name="photos" accept="image/jpeg,.jpg,.jpeg" multiple required><span class="meta" id="ratio-warning"></span><span class="preview-list" id="upload-previews"></span></label><button>Upload photos</button></form>` : `<p class="notice warning">Cloudinary uploads are unavailable until the three staging environment variables are configured.</p>`}${images ? `<div class="images" id="photo-grid">${images}</div><p class="meta" id="order-status"></p>` : `<p class="meta">No direct place images are attached yet.</p>`}</section>
      <section class="panel wide" id="videos"><h2>Video <span class="meta">One active video per place</span></h2>${videoNotices}<p class="meta">Vertical 9:16 is preferred. Keep it around 30 seconds or less. Other proportions and longer clips are allowed with a warning.</p>${videoContent}${input.cloudinaryReady ? `<form class="upload" method="post" enctype="multipart/form-data" action="/admin/places/${place.id}/video"><input type="hidden" name="_csrf" value="${escapeHtml(input.csrfToken)}"><label>${video ? "Replace video" : "Add video"}<input id="video-file" type="file" name="video" accept="video/mp4,video/quicktime,video/webm,.mp4,.mov,.webm" required><span class="meta" id="video-metadata"></span></label><button>${video ? "Replace video" : "Upload video"}</button></form><video class="video-preview" id="video-preview" controls muted hidden></video>` : `<p class="notice warning">Cloudinary video uploads are unavailable until the three staging environment variables are configured.</p>`}</section>
      <section class="panel wide future"><h2>Events</h2><p class="meta">Import screenshots and link reviewed event drafts to this place in <a href="/admin/events">Events</a>.</p></section>
    </div><dialog id="photo-dialog"><button type="button" aria-label="Close">Close</button><img alt="Photo preview"></dialog><script>
    const csrf=${JSON.stringify(input.csrfToken)},placeId=${JSON.stringify(place.id)},grid=document.querySelector('#photo-grid'),status=document.querySelector('#order-status');let dragged;
    const updatePhotoRanks=()=>{if(!grid)return;const cards=[...grid.querySelectorAll('[data-image-id]')],cover=cards.find(card=>card.dataset.cover==='true'),ordered=cover?[cover,...cards.filter(card=>card!==cover)]:cards;cards.forEach(card=>{const badge=card.querySelector('.photo-rank'),rank=ordered.indexOf(card)+1;badge.className='photo-rank'+(rank<=3?' active':'')+(rank===1&&card===cover?' cover':'');badge.textContent=rank<=3?'TUUTI '+rank+(rank===1&&card===cover?' · Cover':''):''})};
    if(grid){updatePhotoRanks();grid.addEventListener('dragstart',e=>{dragged=e.target.closest('[data-image-id]');if(dragged)dragged.classList.add('dragging')});grid.addEventListener('dragend',async()=>{if(!dragged)return;dragged.classList.remove('dragging');dragged=undefined;updatePhotoRanks();const image_ids=[...grid.querySelectorAll('[data-image-id]')].map(x=>x.dataset.imageId);status.textContent='Saving order…';const response=await fetch('/admin/places/'+placeId+'/photos/order',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({_csrf:csrf,image_ids})});status.textContent=response.ok?'Order saved.':'Could not save the new order.'});grid.addEventListener('dragover',e=>{e.preventDefault();const target=e.target.closest('[data-image-id]');if(dragged&&target&&target!==dragged){const box=target.getBoundingClientRect();grid.insertBefore(dragged,e.clientY<box.top+box.height/2?target:target.nextSibling);updatePhotoRanks()}})}
    const dialog=document.querySelector('#photo-dialog');document.addEventListener('click',e=>{const image=e.target.closest('[data-preview]');if(image){dialog.querySelector('img').src=image.dataset.preview;dialog.showModal()}});dialog.querySelector('button').addEventListener('click',()=>dialog.close());
    const files=document.querySelector('#photo-files'),previews=document.querySelector('#upload-previews'),warning=document.querySelector('#ratio-warning');if(files)files.addEventListener('change',()=>{previews.innerHTML='';warning.textContent='';[...files.files].forEach(file=>{const url=URL.createObjectURL(file),img=new Image();img.onload=()=>{if(Math.abs(img.naturalWidth/img.naturalHeight-.8)>.04)warning.textContent+=' '+file.name+' is not approximately 4:5.'};img.src=url;const thumb=document.createElement('img');thumb.src=url;thumb.alt=file.name;previews.append(thumb)})});
    const videoFile=document.querySelector('#video-file'),videoPreview=document.querySelector('#video-preview'),videoMetadata=document.querySelector('#video-metadata');if(videoFile)videoFile.addEventListener('change',()=>{videoMetadata.textContent='';videoPreview.hidden=true;const file=videoFile.files[0];if(!file)return;const url=URL.createObjectURL(file);videoPreview.src=url;videoPreview.onloadedmetadata=()=>{const notes=[videoPreview.videoWidth+'×'+videoPreview.videoHeight,videoPreview.duration.toFixed(1)+' seconds',(file.size/1024/1024).toFixed(1)+' MB'];if(Math.abs(videoPreview.videoWidth/videoPreview.videoHeight-9/16)>.03)notes.push('not approximately 9:16');if(videoPreview.duration>30)notes.push('longer than the recommended 30 seconds');videoMetadata.textContent=notes.join(' · ');videoPreview.hidden=false}});
    </script>`);
}
