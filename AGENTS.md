# Branch workflow

Work only on the `staging` branch. Do not modify, merge, or push to `main`.
After completing a change, commit and push it to `staging`.

Codex builds on `staging`; TUUTI users are on `main`.
